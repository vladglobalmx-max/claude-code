# Performance Budget — Impulso Builder Platform

Regla permanente desde Foundation 3: toda decisión con impacto de rendimiento se documenta con **complejidad aproximada**, **cuellos de botella posibles** y **estrategia de optimización futura** — sin optimizar prematuramente. Este documento es el registro consolidado entre Foundations; el detalle de cada decisión vive también en el ADR correspondiente.

**Objetivo del proyecto:** Impulso debe poder manejar documentos grandes (miles de objetos) sin degradar la experiencia de edición.

## Registro

| # | Foundation | Decisión | Complejidad | Cuello de botella | Optimización futura (no implementada) |
|---|---|---|---|---|---|
| 1 | 2 (Engine) | `tree/objectTree.ts`: buscar/actualizar/eliminar un `SceneObject` por id reconstruye el sub-árbol completo del documento | O(n) por operación, n = total de objects en el documento | Documentos con miles de objects y ediciones frecuentes (ej. drag continuo) generan O(n) trabajo por cada evento | Índice `objectId → ruta` mantenido incrementalmente, para localizar/actualizar en O(profundidad) en vez de O(n) |
| 2 | 2 (Engine) | Undo/redo por snapshot completo del `Project` (no patches) | O(tamaño del Project) en memoria por entrada de historial | Sesiones largas con documentos grandes acumulan memoria proporcional a `historyLimit` × tamaño del documento | Undo/redo basado en patches/comandos inversos (JSON Patch o similar) — requiere definir un formato de patch, hoy deliberadamente opaco (`HistoryEntry.patch: JsonValue`) |
| 3 | 2 (Engine) | `updateInList`/reducers reconstruyen un `Group` con un nuevo objeto (`{...object, children: ...}`) aunque nada dentro de ese group haya cambiado | Rompe "structural sharing" solo para ancestros `group` de cualquier cambio en el documento | Un futuro diffing por referencia (ver #4) no podría confiar en igualdad referencial para `group`s sin cambios | Preservar la referencia del `group` cuando ninguno de sus descendientes cambió (comparar antes de reconstruir) |
| 4 | 3 (Renderer) | `createKonvaRenderer` hace **rebuild completo** de los nodos Konva en cada `projectChanged` (`destroyChildren()` + recrear todo) | O(m) por render, m = total de objects en la página activa | La causa principal de jank en documentos grandes: cualquier cambio, por pequeño que sea (mover un solo object), destruye y recrea TODOS los nodos Konva de la página | Reconciliación incremental: diffear el árbol anterior vs el nuevo por `id` (crear/actualizar/eliminar solo lo que cambió), similar a un diff de DOM virtual. Depende de resolver primero el Riesgo #3 (o de diffear por id+igualdad estructural en vez de por referencia) |
| 5 | 3 (Renderer) | Un único `Konva.Layer` por Stage; cada `Layer` del Document Schema se mapea a un `Konva.Group` (no a un `Konva.Layer`) | O(1) canvases reales por Stage, independiente del número de Layers del documento | Ninguno identificado — es la mitigación, no el problema | N/A — Konva recomienda explícitamente pocos `Layer` reales (cada uno es un canvas propio); mapear 1:1 Document-Layer→Konva.Layer hubiera sido el anti-patrón a evitar |
| 6 | 3 (Renderer) | Conversión de unidades física (mm/in) a píxeles vía una constante fija (`96px/in`) | O(1) | Ninguno a este tamaño | **Parcialmente resuelto en Epic 3**: Export Engine multiplica esa misma base por un factor de escala 1x-4x (no un DPI arbitrario, ver ADR-0012) — sigue siendo la constante fija de pantalla, no un DPI de impresión real; ver fila 13 |
| 7 | Editor Epic 1 (Engine + Renderer) | `dragmove` de un handle de resize/rotación llama una función pura (`computeResizedTransform`/`computeRotatedTransform`) y aplica el resultado directamente al node Konva, SIN `dispatch`; solo `dragend` despacha `resizeObject`/`rotateObject` (un único comando por gesto completo, no por frame) | O(1) por `dragmove`; O(m) por `dragend` (m = objects de la página, mismo rebuild ya documentado en la fila 4) | Ninguno nuevo — mismo patrón preview/commit que ya usa el movimiento (Editor 3); evita multiplicar el costo de la fila 4 por cada frame de un gesto continuo | N/A — ya es la mitigación; solo sería relevante si en el futuro se necesitara sincronizar el resize/rotación en tiempo real con un observador externo (colaboración) |
| 8 | Editor Epic 1 (Renderer) | La caja de manipulación (bounding box + 8 handles + handle de rotación) se reconstruye por completo en cada render de `renderSelectionOverlay()`, igual que el resaltado simple de Editor 2 | O(1) — un único object seleccionado, número fijo de nodos (11) por render | Ninguno a este tamaño | **Resuelto en Fase 7.4** (ver fila 16): la caja compartida sigue siendo O(1) nodos (11, fijo) sin importar cuántos objects estén seleccionados — nunca "un set de handles por object" |
| 16 | Epic 7 / Fase 7.4 (Renderer) | Manipulación grupal (`groupHandles.ts`): el snapshot de members (`GroupMember[]`, medido vía `computeObjectBoundingBox`) se calcula UNA sola vez al iniciar el gesto; cada `dragmove` itera ese snapshot y escribe atributos Konva directamente (sin `dispatch`, sin remedir), un solo `batchDraw()` por frame | O(N) medición una vez al iniciar (N = objects seleccionados); O(N) escrituras de atributo por frame de `dragmove`, sin ninguna medición Konva adicional | Ninguno verificado hasta 100 objects (mover) / 50 objects (resize, rotar) — smoke tests de performance (`groupHandles.performance.test.ts`) confirman que `getSelfRect` se llama exactamente el mismo número de veces antes y después de 30 frames de `dragmove` | Si el número de objects seleccionables simultáneamente creciera órdenes de magnitud (miles), evaluar throttling del `batchDraw()` por frame — no implementado por no haber evidencia de que haga falta a esta escala |
| 9 | Milestone 1 (App) | `Guardar` serializa el `Project` completo (`JSON.stringify` vía `serializeProject`) en cada click, no incrementalmente | O(tamaño del documento), una sola vez por click explícito del usuario (no automático/frecuente) | Documentos muy grandes podrían tardar perceptiblemente al guardar, y `localStorage.setItem` tiene un límite de cuota (~5-10MB típico) que no se maneja todavía | Guardado incremental/diffs, o mover a IndexedDB (sin límite de tamaño práctico) si el tamaño de documento de Sticker Builder llega a acercarse al límite de `localStorage` |
| 10 | Epic 1 (App) | `layersPanel.ts` reconstruía el DOM completo del panel en cada `selectionChanged`, además de en `projectChanged` — descubierto como bug real (rompía el doble-click nativo de renombrado, ver ADR-0010) antes que como problema de rendimiento, pero era ambos: O(n) trabajo de DOM por cada click de selección, n = objects listados | Antes: O(n) por click de selección. Después: O(n) por cambio de contenido (`projectChanged`, ya intrínseco), O(k) por cambio de selección, k = filas cuya clase `.selected` cambia (típicamente 1-2) | Documentos con muchos objects en el panel de capas harían perceptible el costo de reconstruir cada fila en cada click, antes de la corrección | N/A — ya resuelto: `rowElements: Map<ObjectId, HTMLElement>` separa reconstrucción completa (`projectChanged`) de actualización liviana de clase (`selectionChanged`) |
| 11 | Epic 1 (App) | Imágenes embebidas como data URL en `customProperties` (sin Asset Library, ver ADR-0010): el tamaño del `Project` serializado crece proporcionalmente a cada imagen agregada, sin deduplicación ni compresión | O(tamaño total de imágenes embebidas) en el documento serializado y en cada `Guardar`/`Abrir` | Documentos con varias imágenes grandes pueden acercarse al límite de cuota de `localStorage` (~5-10MB típico, ver fila 9) antes de lo que un usuario esperaría de un "sticker simple" | **Resuelto en Epic 2** — ver fila 12: los binarios se movieron a IndexedDB, el `Project` serializado vuelve a ser solo metadata liviana |
| 12 | Epic 2 (App, `packages/asset-library`) | Binarios de Asset en IndexedDB (`AssetBinaryStore`), fuera del `Project` serializado; `ResolvedAssetCache` en memoria resuelve sincrónicamente hacia `resolveAssetSource` tras precargar cada Asset de type "image" al montar/abrir | O(cantidad de Assets tipo imagen) operaciones IndexedDB (asíncronas) en cada "Abrir"/"Nuevo", antes del primer render; O(1) por resolución sincrónica durante el render en sí | Un proyecto con muchas imágenes retrasa el primer render mientras se precargan todas (`Promise.all`) — hoy sin indicador de progreso ni precarga incremental/perezosa | Precarga perezosa (solo las imágenes visibles en el viewport actual) o progresiva con placeholder mientras cada una resuelve, si el número de imágenes por documento crece significativamente |
| 13 | Epic 3 (`packages/export-engine`, PNG) | `rasterizeProjectToPng` construye un `Konva.Stage` headless completo (mismo costo de creación de nodos que el render interactivo, fila 4) y pide un canvas a `pixelRatio` (1x-4x) — la memoria del canvas resultante escala con el CUADRADO del factor de escala (4x = 16× los píxeles de 1x) | O(m) construcción de nodos (m = objects de la página) + O(ancho×alto×escala²) memoria del canvas rasterizado | Documentos grandes exportados a 4x pueden acercarse a límites reales de memoria del navegador | Mitigado con un error explícito (`out_of_memory`, ver ADR-0012) en vez de una pestaña colgada — sin estrategia de tiling/chunking para canvases extremadamente grandes, deliberadamente fuera de alcance v1 |
| 14 | Epic 3 (`packages/export-engine`, SVG) | `buildSvgDocument` recorre el documento una vez (O(total objects)) y embebe cada Asset de tipo imagen referenciado como data URI base64 (con cache por `assetId`, sin recodificar si se repite) | O(total objects) + O(assets únicos referenciados) conversiones Blob→base64 (asíncronas, en paralelo) | Base64 infla el tamaño del binario original ~33% dentro del string SVG resultante; sin compresión ni deduplicación entre Assets distintos con contenido idéntico | Heredada de Asset Library (fila 12/ADR-0011) — no se resuelve aparte en esta épica |
| 15 | Epic 4 (`builtInTemplates.ts`) | Sembrado perezoso de los 3 Templates built-in: cada uno rasteriza un PNG completo (mismo costo que la fila 13, escala 1x) vía `exportProject`, disparado en el primer "Nuevo proyecto" real de la sesión | O(1) por Template built-in (contenido mínimo), 3 rasterizaciones una sola vez por sesión (idempotente por id de catálogo) | Ninguno a este tamaño — el sembrado es perezoso y se salta por completo si ya existen los 3 ids | N/A — ya mitigado por el diseño (perezoso + idempotente + `.catch()` no bloqueante, ver ADR-0013) |
| 16 | Epic 4/5 (`saveAsTemplateDialog.ts`/`app.ts`, Guardar) | Cada "Guardar como plantilla" y cada "Guardar" explícito en el editor (Epic 5) rasteriza un PNG completo del proyecto actual (mismo costo que la fila 13, escala 1x, fondo sólido) para generar/actualizar el thumbnail | O(m) construcción de nodos (m = objects de la página activa), una vez por click explícito | Documentos grandes podrían notar una demora perceptible al hacer click en "Guardar", no medida todavía; relevante si en el futuro se implementa autosave (ya resuelto para Sticker Builder como componente interno desde Epic 8, ver ADR-0019; para un futuro segundo módulo, ver `docs/product/THOREN_PRODUCT_BACKLOG_V2.md`) con una cadencia automática en vez de un click explícito | Separar la cadencia de "guardar el `Project`" de "regenerar el thumbnail" si autosave se implementa — no es necesario mientras el guardado siga siendo explícito y poco frecuente |
| 17 | Epic 7 / Fase 7.2 (`@impulso/engine`, `dispatchBatch`) | Un batch de N comandos ejecuta N reducers en secuencia sobre un acumulador local, y valida el `Project` completo (`ProjectSchema.safeParse`) UNA sola vez al final (no N veces) — evita repetir la validación completa del documento por cada comando del batch | O(N) reducers + O(1) validaciones completas de schema (en vez de O(N) validaciones), N = comandos del batch | Un batch con miles de comandos sigue siendo O(tamaño del Project) por la única validación final — no medido a esa escala; Alignment/Distribution hoy generan como máximo 1 comando por object seleccionado, no miles | Si en el futuro un batch pudiera tener miles de comandos (ej. una operación sobre todo un documento grande), evaluar si la validación final por schema completo sigue siendo suficientemente rápida, o si conviene validar incrementalmente |
| 18 | Epic 7 / Fase 7.2 (`renderer-konva`, `computeObjectBoundingBox`/Alignment) | Alignment mide la caja real de cada object seleccionado vía Konva (`getSelfRect()`/`getClientRect()`, ya el costo existente de `computeManipulationBox`, ver ADR-0008) una vez por object por operación — no cachea bounding boxes entre operaciones sucesivas | O(k) mediciones Konva por operación, k = objects seleccionados | Con selecciones muy grandes (cientos de objects) y operaciones repetidas en sucesión rápida, cada una vuelve a medir todo desde cero | Cache de bounding boxes invalidado por `projectChanged`/`selectionChanged`, si en el futuro las selecciones típicas crecen lo suficiente para que remedir sea perceptible |
| 19 | Epic 8 (`@impulso/project-library`, `ProjectSaveCoordinator`) | Resuelve exactamente la preocupación anticipada en la fila 16 ("relevante si en el futuro se implementa autosave... con una cadencia automática en vez de un click explícito"): el guardado principal (thumbnail + `save`) y el recovery rápido corren cada uno detrás de SU PROPIO debounce (1200ms / 400ms) — una ráfaga de N `notifyChange()` (tipeo, nudge repetido, drag) nunca dispara más de un guardado principal + un recovery, nunca uno por cambio (probado explícitamente en `saveCoordinator.test.ts`: "múltiples cambios se consolidan en un solo guardado" / "una ráfaga rápida consolida el recovery en una sola escritura"). Un guardado en curso nunca se solapa con otro (`inFlightSave` de un solo vuelo) | O(1) guardado principal + O(1) recovery por ráfaga de cambios, independientemente de N | El guardado principal sigue regenerando el thumbnail completo (mismo costo ya documentado en la fila 16) en CADA ciclo de autosave, no solo en un click explícito — en un documento muy grande con ediciones frecuentes y espaciadas más de 1200ms entre sí, esto podría notarse más seguido que antes de Epic 8 (no medido a esa escala) | Si se demuestra perceptible: desacoplar "persistir el `Project`" de "regenerar el thumbnail" en el mismo ciclo (ver fila 15 de la sección de Workspace/miniaturas del ADR de esta épica para la decisión tomada de todos modos de NO bloquear el guardado principal por un fallo del thumbnail) — no implementado por no haber evidencia de que haga falta a esta escala; no se introducen Web Workers sin medición real que lo justifique (ver enunciado de producto de esta épica) |
| 20 | Epic 8 (`apps/sticker-builder`, `workspace.ts`) | `refresh()` (recarga descriptores + banner de recovery) ahora tiene guarda de "único vuelo" (`refreshInFlight`) — encontrado como bug real durante la verificación E2E de esta épica (dos llamadas concurrentes al montar la Workspace —una de `mountWorkspace` y otra de `shell.ts`— se solapaban y duplicaban filas del banner de recovery), corregido con el mismo patrón que `ProjectSaveCoordinator.startSave()` | O(1) — una llamada concurrente espera la misma ejecución en vez de iniciar una segunda | Ninguno nuevo — ya es la mitigación | N/A |
| 21 | Epic 9 / Fase 9.2 (`packages/print-engine`, `renderPrintJob`) | Un `PrintJob` de N páginas se procesa **secuencialmente**, nunca en paralelo: `renderPrintJob` expone sus páginas como un **generador asíncrono** (`for await...of`) — cada `renderPrintPage` destruye su propio Stage offscreen (`finally { stage.destroy() }`) ANTES de que el consumidor pida la siguiente página, y `exportPrintJobToPdf` libera el canvas de cada página tras incrustarla en el PDF antes de rasterizar la siguiente. Nunca hay dos canvases físicos de página completos vivos en memoria a la vez, sin importar cuántas páginas tenga el `PrintJob` | O(1) canvases de página simultáneos, independiente de N páginas; O(ancho×alto×targetPpi²) memoria por UNA página a la vez (el mismo escalado cuadrático ya documentado en la fila 13 para PNG, ahora también aplicado a cada página de un PDF) | Un `PrintJob` con muchas páginas a alta resolución sigue siendo lento en tiempo total (secuencial, no paralelo) — deliberado: procesar en paralelo multiplicaría el pico de memoria simultánea exactamente por lo que esta estrategia evita | Si el tiempo total (no el pico de memoria) se vuelve el cuello de botella medido, evaluar paralelismo acotado (ej. 2-3 páginas a la vez) solo si el presupuesto de memoria del dispositivo lo permite — no implementado por no haber evidencia de que haga falta a esta escala |
| 22 | Epic 9 / Fase 9.2 (`packages/print-engine`, `memory.ts`/`assetImageCache.ts`) | `estimateMemoryBytes` (extendido de Fase 9.1) suma: el canvas de raster principal (ancho×alto×4 bytes RGBA) × `simultaneousPages` (hoy siempre 1, ver fila 21) × `MEMORY_OVERHEAD_FACTOR` (2.5, cubre buffers temporales/codificación) + `cachedImageBytes` (el cache de imágenes decodificadas de `assetImageCache.ts`, que vive UNA vez por exportación completa, reutilizado entre todas las páginas — nunca decodifica el mismo Asset dos veces). Preflight (`raster_too_large`, Fase 9.1) sigue siendo el único punto de bloqueo — se comprobó que un chequeo adicional a nivel de `renderPrintJob` sería inalcanzable en la práctica, porque el box que usa Preflight (`MediaBox`) siempre es ≥ al que rasteriza esta fase (`BleedBox`), así que Preflight siempre bloquea primero; el chequeo redundante se eliminó deliberadamente (ver ADR-0022) | O(1) estimación por `PrintJob` (no por página, dado el procesamiento secuencial de la fila 21) | El factor 2.5x y los umbrales recommended/warning/blocking (`MemoryRiskLevel`) siguen sin una medición empírica de dispositivos/navegadores reales de usuarios — igual que la fila 13 de Epic 3 | Ajustar los umbrales con datos reales en Fase 9.5, si la evidencia (reportes de fallo real, no solo estimación teórica) lo justifica |

| 23 | Epic 9 / Fase 9.3 (`packages/print-engine`, `composeFinalCanvas`/`composeCanvasOverlays.ts`) | Componer marcas de corte y cut path NUNCA dispara un segundo render de Konva: `composeFinalCanvas` reutiliza el canvas de contenido YA rasterizado (`renderPrintPage`), y solo crea un canvas de `MediaBox` adicional (`createMediaCanvasWithContent`) cuando `needsOverlayComposition` es verdadero (`cropMarks.enabled` o `cutPath.mode !== "none"`) — cuando ninguno de los dos está activo, se devuelve el canvas de contenido tal cual, sin gastar memoria/tiempo extra. En PDF, las marcas del job se calculan UNA sola vez (no dependen del contenido de cada página) y se reutilizan en el loop de páginas; el cut path sí se recalcula por página (depende del die-line propio de cada una) | O(1) canvas adicional (o cero) por página, nunca O(2) renders de Konva | Un `PrintJob` con marcas+cut path activos en TODAS sus páginas paga un canvas de `MediaBox` completo por página (mismo escalado cuadrático de la fila 13/21) — inherente a que el `MediaBox` es más grande que el `BleedBox` cuando hay marcas, no una ineficiencia evitable | Ninguna identificada — el diseño ya evita el único desperdicio real (un segundo render de Konva); el costo restante es geometría física inherente |
| 24 | Epic 9 / Fase 9.3 (`packages/print-engine`, Preflight — `checkSafeAreaForPage`/`checkCutPathForPage`) | Los nuevos chequeos de Preflight son funciones PURAS sobre datos ya en memoria (bounding boxes calculados vía composición afín, nunca medidos con Konva) — a diferencia de `resolution_insufficient`/`font_unavailable` (Fase 9.1), que sí hacen I/O asíncrono por object (`resolver.resolve`/`fontChecker.check`), safe area y cut path no agregan ninguna espera nueva al loop existente por página | O(objects de la página) para safe area (una unión de bounding boxes por `group`, ya el mismo costo que Alignment/selección, fila 18) + O(1) para cut path (un único die-line resuelto, nunca todos los objects) | Ninguno identificado a la escala típica de un documento de sticker (decenas de objects) — no medido con miles de objects por página | Si un documento con miles de objects por página demostrara ser lento en Preflight, evaluar cachear bounding boxes entre el chequeo de safe area y cualquier otro paso que ya los necesite (mismo patrón anticipado en la fila 18) |
| 25 | Epic 9 / Fase 9.4 (`packages/print-engine`, `raster/pieceRasterCache.ts`, `exportImpositionToPdf`/`exportImpositionToPng`) | Una pieza de imposición se rasteriza/embebe **UNA sola vez por página de origen**, sin importar cuántas copias tenga en total (`quantity`) ni en cuántas hojas se reparta (`sheetCount`) — verificado EMPÍRICAMENTE, no solo argumentado: `raster/impositionPerformance.test.ts` confirma `renderPageToStageMock` llamado exactamente una vez con 500 piezas/10 hojas. `computeImpositionLayout` (geometría pura) completa en <500ms a esa escala; la exportación completa (render mockeado, encoding real de pdf-lib/Canvas) completa en <10s | O(1) rasterizaciones de Konva por página de origen, independiente de `quantity`/`sheetCount`; O(quantity) operaciones de dibujo/composición puramente aritméticas (posicionar el mismo raster ya decodificado) | Límites de producto explícitos (`MAX_IMPOSITION_SHEETS = 200`, `MAX_IMPOSITION_PIECES = 2000`, ver ADR-0024) — un orden de magnitud por encima del rango medido (500/10), sin medición real de dispositivos de usuarios en ese rango superior | Medir con dispositivos/navegadores reales cerca de los límites de producto en Fase 9.5, y ajustar esos límites (o el presupuesto de memoria, fila 22) con evidencia real si hace falta |
| 26 | Epic 9 / Fase 9.4 (`apps/sticker-builder`, `productionPreview.ts`) | El Production Preview rasteriza la pieza de origen UNA vez por hoja mostrada (no una vez por copia) a `PREVIEW_PPI = 72` fijo — deliberadamente menor al `targetPpi` real de producción, evitando pagar el costo de rasterizar a resolución de impresión solo para mostrarlo en pantalla a una fracción de su tamaño | O(1) rasterización por cambio de hoja/página mostrada; sin cache entre navegaciones (cada `render()` dispara un único raster nuevo) | Ninguno identificado — no hace falta cache porque el costo por render ya es bajo a `PREVIEW_PPI` fijo y la navegación entre hojas no es de alta frecuencia (clicks explícitos del usuario, no un gesto continuo) | Si la navegación entre hojas demostrara ser perceptiblemente lenta con documentos grandes, evaluar un cache del último raster mostrado — no implementado por no haber evidencia de que haga falta |

| 27 | Epic 9 / Fase 9.5 (Hardening) | Verificación de performance/memoria/resource-leaks del wizard "Exportar para impresión" en Chromium REAL (Playwright, sin mocks), distinta de las filas 25/26 (que miden con Konva mockeado en `vitest`/jsdom — orquestación pura, no rasterización real) | O(1) rasterizaciones reales por pieza de origen (confirmado indirectamente: el tiempo de un ciclo con 200 copias es del mismo orden que uno con 4, ver observación abajo) | Un solo entorno/máquina midió esto — nunca se afirma como cota universal | Repetir esta medición si el pipeline de rasterización cambia de forma significativa (ej. reconciliación incremental de Konva, fila 4) |
| 28 | Epic 9 / Fase 9.5 (Hardening) | Dato OBSERVADO (no estimado): un ciclo completo real (abrir wizard → perfil "Sticker Sheet" → 200 copias imposicionadas → preview real → Preflight → exportar PDF real → descargar) en Chromium headless, máquina de este entorno de desarrollo: **526ms**. Confirma la reutilización de raster de la fila 25 incluso a través de la UI completa (no solo la llamada directa al exportador) | — | El valor depende enteramente del hardware/carga de la máquina que lo mide — nunca se usa como bloqueo estricto de CI (`e2e/production-export-hardening.spec.ts` usa un umbral generoso de 30s, deliberadamente muy por encima de lo observado) | Repetir la medición si se sospecha una regresión real; no ajustar el umbral del test hacia abajo sin evidencia de que la máquina de CI sea consistentemente más rápida |
| 29 | Epic 9 / Fase 9.5 (Hardening) | Memoria OBSERVADA (no estimada) vía `performance.memory` (API no estándar, presente en Chromium sin flags, valores cuantizados por motivos de seguridad) — `usedJSHeapSize` antes de cualquier ciclo: **8.7MB**; después de 5 ciclos completos consecutivos (abrir/config/preview/Preflight/exportar/descargar/cerrar, 30 copias c/u): **17.0MB**. Sin forzar GC (Playwright no habilita `--expose-gc` por defecto), por lo que esta lectura es ruidosa por diseño — no se afirma que el crecimiento observado sea 100% atribuible a los ciclos (podría incluir estructuras del propio harness de Playwright/DevTools) | — | Esta es una señal aproximada, NUNCA una medición precisa de memoria real de proceso — distinta de la estimación teórica de la fila 22 (`estimateMemoryBytes`, un modelo, no una observación). No se puede diferenciar aquí cuánto de ese crecimiento es GC pendiente (no un leak) vs retención real | Si se necesita una medición de memoria más rigurosa en el futuro, considerar el protocolo CDP (`Performance.getMetrics`) con GC forzado explícito antes de cada lectura — no implementado en esta fase por no haber evidencia de que la señal actual (que no muestra crecimiento desbocado) sea insuficiente |
| 30 | Epic 9 / Fase 9.5 (Hardening) | Resource leaks del wizard verificados EMPÍRICAMENTE (no solo argumentados): `URL.createObjectURL`/`URL.revokeObjectURL` reales instrumentados vía monkey-patch instalado antes de cargar la app (`e2e/production-export-hardening.spec.ts`) — creates===revokes tras 3 ciclos completos de éxito y tras 3 ciclos de cancelación repetida; conteo de `<canvas>` en el DOM vuelve exactamente a la línea base (el canvas del editor principal) después de cada cierre del diálogo, en ambos escenarios. Ningún bug de leak encontrado — el cleanup ya existente (`preview.destroy()` idempotente, `triggerBrowserDownload`'s `setTimeout(() => revokeObjectURL, 0)`, `controller.close()` abortando exportaciones en curso) ya era correcto antes de esta verificación | O(1) recursos vivos simultáneos, independiente del número de ciclos repetidos | Solo se instrumentaron object URLs y canvases — timers/listeners/`ImageBitmap` no tienen un contador global inspeccionable de forma tan directa desde Playwright; se revisó manualmente el código fuente (ver Corregido del CHANGELOG de esta fase) y no se encontraron timers/listeners persistentes fuera del ciclo de vida del diálogo | Si en el futuro se agregan nuevos recursos (workers, `ImageBitmap` explícitos, etc.), extender la misma instrumentación de monkey-patch a sus APIs de creación/liberación |

## Cómo se usa este documento

Antes de cerrar un Foundation, revisar si alguna decisión nueva tiene impacto de rendimiento y agregar una fila aquí (y la sección "Rendimiento" correspondiente en su ADR). No es una lista de tareas pendientes — es un mapa de "dónde están los cuellos de botella conocidos y por qué no se resolvieron todavía", para que la próxima vez que rendimiento sea un problema real y medido, ya exista un plan escrito en vez de tener que investigarlo desde cero.
