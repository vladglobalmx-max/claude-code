{
  "name": "impulso-builder-platform",
  "private": true,
  "version": "0.0.0",
  "description": "Monorepo de Impulso Builder Platform. Impulso Engine y sus módulos (Sticker Builder, y futuros) viven aquí.",
  "packageManager": "pnpm@9.7.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "test:coverage": "turbo run test:coverage",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.1.1",
    "typescript": "^5.5.4"
  }
}
