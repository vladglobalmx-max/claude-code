{
  "name": "@impulso/project-library",
  "version": "0.2.0",
  "private": true,
  "description": "Project Library oficial de Impulso Platform: administración de múltiples proyectos guardados (Workspace), reutilizable por cualquier módulo (Sticker Builder, Planner Builder, Coloring Book Builder...). Un Project ya es su propio descriptor de catálogo — este paquete solo separa el listado liviano de su contenido pesado.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@impulso/document-schema": "workspace:*",
    "@impulso/engine": "workspace:*",
    "@impulso/storage-kit": "workspace:*"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.0.5",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.1",
    "tsup": "^8.2.4",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
