/**
 * Tamaño de página compartido por los tres arquetipos de esta receta — el
 * mismo producto físico (una etiqueta cuadrada), tres estructuras
 * distintas encima. Ligeramente mayor que el default de Fase 1 (240) para
 * darle al anillo de texto del Arquetipo A margen suficiente para no
 * recortarse contra el borde.
 */
export const PAGE_SIZE = 260;
